1、编写控件验证的HELPER类，类似数据库的数据验证。
2、验证插入的工程是否重复,（老师这么写，我觉得判断似乎可以放入BLL库中，
               作为插入的判断DAL库只要增加一个查找函数就可以了）自己做的时候可以试试看。
3、自己编写的ORM增加返回ID号的操作。参照老师的方法，利于最后添加数据，减少数据库的访问次数，减少负担。
4、如果有控件级联查询，下一级的事件绑定要在上一级初始化完成后才进行。程序中协议类型下拉框的内容依赖于设备
    类型的选择，所以要在设备类型下拉框初始化完成后再进行事件绑定，否则报错。